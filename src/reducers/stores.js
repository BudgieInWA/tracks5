export function getTradesThatHappen(state) {
  // All trains that are stopped and are offering to trade.

  // All player offered trades
  return [];
}

export function executeTrades(stores, trades) {

  return stores;
}
